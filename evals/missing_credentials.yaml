name: list_pto_requests returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: list_pto_requests
  args: {}
  expect:
    is_error: true
timeout: 30s
